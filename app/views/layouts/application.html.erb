<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Bugstream" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
    <%= yield %>
    <%= render "shared/bundle", entrypoint: "application" %>
  </body>
</html>
